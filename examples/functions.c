/**
 * This illustrates functions
 * 
*/
#include <stdio.h>

int _pow(int , int);

int main(){

    _pow(2,1);
    return 0;
}

// the old syntax of function
// declaration before ANSI C syntax
int _pow(base, n)
int base, n;
{
  // so logic for power computation

  return 1 ; // some number
}